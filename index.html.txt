<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AntiBan Maroc — Roblox Fan Hub</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #0f0520;
      color: #fff;
      min-height: 100vh;
    }

    /* TOPBAR */
    .topbar {
      background: #1a0a35;
      border-bottom: 2px solid #c4a35a;
      padding: 0 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 52px;
    }
    .logo {
      font-size: 15px;
      font-weight: 600;
      color: #c4a35a;
      letter-spacing: 1px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .logo-star {
      width: 20px; height: 20px;
      background: #c4a35a;
      clip-path: polygon(50% 0%,61% 35%,98% 35%,68% 57%,79% 91%,50% 70%,21% 91%,32% 57%,2% 35%,39% 35%);
    }
    .topbar-right { font-size: 12px; color: #7a5a9a; }

    /* ZELLIGE BAR */
    .zellige-bar {
      height: 10px;
      background: repeating-linear-gradient(
        90deg,
        #c4a35a 0px, #c4a35a 10px,
        #8b0000 10px, #8b0000 20px,
        #1a0a35 20px, #1a0a35 30px
      );
    }

    /* HERO */
    .hero {
      background: #1a0a35;
      padding: 48px 24px 40px;
      text-align: center;
      border-bottom: 1px solid #2d1a50;
    }
    .hero h1 {
      font-size: 32px;
      font-weight: 700;
      color: #c4a35a;
      margin-bottom: 8px;
    }
    .hero-sub {
      font-size: 14px;
      color: #9a7abf;
      margin-bottom: 32px;
      letter-spacing: 0.5px;
    }

    /* ANTIBAN BADGE */
    .antiban-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(95,220,122,0.1);
      border: 1px solid #5fdc7a;
      border-radius: 20px;
      padding: 6px 16px;
      margin-bottom: 32px;
    }
    .antiban-dot {
      width: 8px; height: 8px;
      border-radius: 50%;
      background: #5fdc7a;
      animation: blink 1.4s infinite;
    }
    @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.3} }
    .antiban-label { font-size: 12px; color: #5fdc7a; font-weight: 600; }

    /* LOGIN CARD */
    .login-card {
      background: #0f0520;
      border: 2px solid #c4a35a;
      border-radius: 12px;
      max-width: 360px;
      margin: 0 auto;
      overflow: hidden;
    }
    .login-header {
      background: #c4a35a;
      padding: 10px 16px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .login-header-title { font-size: 12px; font-weight: 600; color: #0f0520; }
    .login-dots { display: flex; gap: 5px; }
    .login-dot { width: 8px; height: 8px; border-radius: 50%; background: #0f0520; opacity: 0.4; }

    .login-body { padding: 24px 20px; }

    .field-label { font-size: 12px; color: #9a7abf; margin-bottom: 6px; }
    .field-input {
      width: 100%;
      background: #2d1a50;
      border: 1px solid #5a3a80;
      border-radius: 6px;
      padding: 10px 12px;
      font-size: 13px;
      color: #c4a35a;
      margin-bottom: 16px;
      outline: none;
      font-family: inherit;
    }
    .field-input:focus { border-color: #c4a35a; }

    .btn-main {
      width: 100%;
      background: #c4a35a;
      color: #0f0520;
      font-size: 13px;
      font-weight: 700;
      padding: 12px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      letter-spacing: 0.5px;
      margin-bottom: 12px;
      transition: background 0.2s;
    }
    .btn-main:hover { background: #d4b36a; }

    .divider {
      display: flex; align-items: center; gap: 10px; margin: 4px 0 12px;
    }
    .divider-line { flex: 1; height: 1px; background: #2d1a50; }
    .divider-text { font-size: 11px; color: #5a3a80; }

    .btn-outline {
      width: 100%;
      background: transparent;
      color: #c4a35a;
      font-size: 13px;
      padding: 11px;
      border: 1px solid #c4a35a;
      border-radius: 6px;
      cursor: pointer;
      letter-spacing: 0.5px;
      transition: background 0.2s;
    }
    .btn-outline:hover { background: rgba(196,163,90,0.08); }

    /* CONTENT */
    .content { padding: 32px 24px; max-width: 700px; margin: 0 auto; }

    .welcome-box {
      background: #1a0a35;
      border: 1px solid #2d1a50;
      border-left: 3px solid #c4a35a;
      border-radius: 8px;
      padding: 16px 18px;
      margin-bottom: 28px;
    }
    .welcome-box-title { font-size: 14px; font-weight: 600; color: #c4a35a; margin-bottom: 6px; }
    .welcome-box-text { font-size: 13px; color: #9a7abf; line-height: 1.6; }

    .section-label {
      font-size: 12px;
      font-weight: 600;
      color: #c4a35a;
      letter-spacing: 1px;
      text-transform: uppercase;
      margin-bottom: 14px;
    }

    .cards-row {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
      margin-bottom: 28px;
    }
    .info-card {
      background: #1a0a35;
      border: 1px solid #2d1a50;
      border-radius: 8px;
      padding: 16px 12px;
      text-align: center;
    }
    .info-card-label { font-size: 11px; color: #9a7abf; margin-bottom: 6px; }
    .info-card-val { font-size: 20px; font-weight: 700; color: #c4a35a; }

    .tag-row { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 28px; }
    .tag {
      font-size: 12px;
      padding: 5px 12px;
      border-radius: 20px;
      border: 1px solid;
    }
    .tag.gold { color: #c4a35a; border-color: #c4a35a; background: rgba(196,163,90,0.08); }
    .tag.purple { color: #9a7abf; border-color: #5a3a80; background: rgba(90,58,128,0.15); }
    .tag.red { color: #e07070; border-color: #8b0000; background: rgba(139,0,0,0.1); }

    /* FOOTER */
    .footer {
      background: #1a0a35;
      border-top: 1px solid #2d1a50;
      padding: 14px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer-text { font-size: 11px; color: #5a3a80; }
    .footer-brand { font-size: 12px; font-weight: 600; color: #c4a35a; letter-spacing: 0.5px; }
  </style>
</head>
<body>

  <div class="topbar">
    <div class="logo">
      <div class="logo-star"></div>
      AntiBan Maroc
    </div>
    <div class="topbar-right">roblox fan hub · by pablo</div>
  </div>

  <div class="zellige-bar"></div>

  <div class="hero">
    <h1>Anti-Ban Morocco Vibes</h1>
    <p class="hero-sub">ton hub roblox marocain — dkol w enjoy</p>

    <div class="antiban-badge">
      <div class="antiban-dot"></div>
      <span class="antiban-label">anti-ban morocco vibes actif</span>
    </div>

    <div class="login-card">
      <div class="login-header">
        <span class="login-header-title">connexion · roblox fan hub</span>
        <div class="login-dots">
          <div class="login-dot"></div>
          <div class="login-dot"></div>
          <div class="login-dot"></div>
        </div>
      </div>
      <div class="login-body">
        <div class="field-label">username roblox</div>
        <input class="field-input" type="text" placeholder="PlayerMaroc_01" />

        <div class="field-label">mot de passe</div>
        <input class="field-input" type="password" placeholder="••••••••" />

        <button class="btn-main" onclick="alert('mrhba bik f antiban maroc!')">dkol l-roblox</button>

        <div class="divider">
          <div class="divider-line"></div>
          <span class="divider-text">wella</span>
          <div class="divider-line"></div>
        </div>

        <button class="btn-outline" onclick="window.open('https://www.roblox.com','_blank')">sir l-roblox.com rasmi</button>
      </div>
    </div>
  </div>

  <div class="zellige-bar"></div>

  <div class="content">

    <div class="welcome-box">
      <div class="welcome-box-title">mrhba bik f anti-ban morocco vibes!</div>
      <div class="welcome-box-text">hada fan hub — machi site rasmi d roblox. sir roblox.com bach dkol b acc dyalk rasmi.</div>
    </div>

    <div class="section-label">stats d l-hub</div>
    <div class="cards-row">
      <div class="info-card">
        <div class="info-card-label">players</div>
        <div class="info-card-val">4.2k</div>
      </div>
      <div class="info-card">
        <div class="info-card-label">rang pablo</div>
        <div class="info-card-val">#1</div>
      </div>
      <div class="info-card">
        <div class="info-card-label">anti-ban</div>
        <div class="info-card-val" style="color:#5fdc7a;">actif</div>
      </div>
    </div>

    <div class="section-label">vibes</div>
    <div class="tag-row">
      <span class="tag gold">morocco</span>
      <span class="tag gold">zellige</span>
      <span class="tag purple">roblox fan</span>
      <span class="tag purple">gaming</span>
      <span class="tag red">daba daba</span>
      <span class="tag gold">by pablo</span>
      <span class="tag purple">blad</span>
    </div>

  </div>

  <div class="footer">
    <span class="footer-text">fan site — machi rasmi</span>
    <span class="footer-brand">by pablo · antiban maroc</span>
  </div>

</body>
</html>
